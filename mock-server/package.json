{
  "name": "mock-server",
  "version": "0.0.1",
  "description": "판정 시스템 REST API Mock 서버 (NestJS)",
  "private": true,
  "scripts": {
    "test": "npm run build && node --test \"test/*.test.cjs\"",
    "start": "nest start",
    "dev": "nest start --watch",
    "build": "nest build"
  },
  "dependencies": {
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "@nestjs/swagger": "^12.0.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "uuid": "^14.0.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^12.0.3",
    "@nestjs/schematics": "^12.0.3",
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.0",
    "source-map-support": "^0.5.21",
    "typescript": "^6.0.2"
  }
}
