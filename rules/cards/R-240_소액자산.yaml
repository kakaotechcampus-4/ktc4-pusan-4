# G4 · 취득가액 100만원 이하 — 즉시 비용 처리할지 자산으로 등재할지
#
# 시행령 §67④ 는 100만원 이하 감가상각자산을 "필요경비로 계상한 것에 한정하여"
# 산입한다고 읽힌다. 계상 여부가 사용자의 선택이라 되묻기로 둔다.
# ⚠️ 이 문언은 2차 자료로만 확인했다. verified 를 달지 않았다. 9/18 세무사 확인 대상.
#
# ⚠️ 시행령 §67⑦4 의 개인용 컴퓨터 특례와 경계가 겹칠 수 있다(평가셋 E-045 보류).
#    그 예외 범위를 확인하기 전까지 금액 상한만으로 가른다.
#
# 생활용품은 match 에서 뺀다. 다이소류 소모품은 감가상각자산이 아니어서 §67④ 가
# 적용될 자리가 아닌데, 카테고리만으로 걸면 1만원짜리에도 자산 되묻기가 붙어
# R-207 이 응답으로 확정한 가능을 되돌린다(평가셋 E-053). 100만원을 넘는
# 생활용품은 R-241 이 그대로 받는다.
#
# 응답 전까지는 판정이 확인필요로 내려간다. 선택지가 verdict·account 를 들지
# 않아도 엔진(JudgmentEngine.AMOUNT_BEARING_ATTRIBUTES)이 `자산`·`즉시상각` 키를
# 당해 경비 '금액'을 바꾸는 축으로 보기 때문이다. R-242 는 G2 가 이미 확인필요라
# 이 강등이 드러나지 않지만, 이 카드는 G2 가 가능으로 확정한 거래 위에 얹히므로
# 그 확정을 되돌린다(평가셋 E-041). §67④ 가 사용자 선택을 요구하는지 확인되기
# 전까지는 그 강등을 감수한다.
id: R-240
version: 2
gate: G4
priority: 510
effective_period: { start: 2024-01-01, end: null }
match:
  category: [사무용품, 전자기기, 온라인쇼핑]
  amount_max: 1000000
citations:
  - { id: 소득세법시행령-67-4 }
question:
  code: SMALL_ASSET_EXPENSING
  text: "취득가액 100만원 이하 물품입니다. 이번 과세기간에 전액 비용으로 처리할까요?"
  fact_type: 즉시상각여부
  group_by: transaction
  options:
    - { value: 전액 비용 처리, 즉시상각: true }
    - { value: 자산으로 등재, 자산: true, 내용연수: 5, 상각방법: 정액법, 자산대장_등재: true }
review: { by: 미검수, date: 2026-09-15 }
