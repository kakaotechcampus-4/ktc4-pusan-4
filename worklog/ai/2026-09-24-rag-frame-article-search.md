# RAG 검색 개선 — 기본 조문 상시 투입과 법령 조 단위 검색

- 브랜치: feature/rag-architecture
- 커밋: da5a1e4..c73aef4 (5개)
- 주요파일: ai/pipeline/search.py, ai/pipeline/select.py, ai/eval/run_draft.py, docs/rag-eval.md

같은 push 에 develop 에서 받은 병합 커밋들(PR #14~#51)과 2944b20(구조도 수정)이 함께 올라간다.
아래는 위 5개만 다룬다.

## 한 일

- RRF 동점을 (statute_id, section, seq) 로 깨고 점수를 float8 로 받는다 (da5a1e4)
- 법령·행정규칙 청크에서 개정 표기 `<개정 …>` 와 한자 병기 `(家事)` 를 걷어낸다 (5235738).
  로컬 DB 는 법령·행정규칙만 `reindex --full` 로 다시 색인했다
- 소득세법 27·33조를 검색 없이 '기본' 위계로 항상 후보에 붙이고(`search.retrieve`), 법령은
  조 상위 k 개를 골라 그 조의 잎을 각자 라벨로 넘긴다(`search.pick`). 조 전문 맥락(`expand`)은
  없앴고 같은 조 머리말은 한 번만 찍는다. 940909 에는 33-1-9 를 뺀다 (9f68a45)
- run_draft 가 질의 계획을 `eval/.plans.json` 에 고정하고, `--search-only` 로 후보 재현율을
  틀(27·33조)·틀 밖으로 나눠 잰다 (b38acf8)
- docs/rag-eval.md 에 진단과 단계별 수치를 남기고 architecture.md·CONTEXT.md 를 고쳤다 (c73aef4)

결과(rag-eval.md §9): 골든셋 4/10 → 7/10, 카드 32키 후보 재현율 2/51 → 47/51,
8키 3회 게이트 4 → 6~8/8 · 근거 적중 1 → 6~7/14 · 오적용 14~15 → 9~11.

## 왜 이렇게 했나

- 카드 키별 인용 51건 중 44건이 27-1·33-1-* 인데 주제 질의로는 수백 위였다. 검색으로 찾을
  대상이 아니라 게이트(G1·G2)의 전제라 검색 없이 붙였다
- 벡터·키워드 상위 30이 거의 안 겹쳐 RRF 가 각자 top-4 를 끼워 넣고 있었고, 한 조의 형제 호가
  같은 머리말(개정일 목록 포함)을 달아 상위를 다 먹었다. k 를 올려도 4/10 그대로였다(시뮬레이션)
- 질의 계획을 고정하기 전에는 rewrite() 비결정성 때문에 게이트가 3~6/8 로 흔들려 1~2 차이를
  판별할 수 없었다

## 남은 것 · 아는 문제

- G1 검증이 "33조 아무 호나 있으면 G1" 이라 기본 조문이 늘 붙는 지금은 G1 을 못 막는다.
  32키 1회에서 7개 카테고리가 G1 으로 갔다(카드는 G2). CONTEXT.md §G1 표에 맞춰야 한다
- 32키 선택누락 26건 중 20건이 33-1-5 를 안 고른 것이다
- 해외SaaS 는 27-1 과 33-1-13 을 같이 붙여 반대 결론(불가)이 난다
- RC-009 must_not 2건(법인세법 근거 고시)은 그대로다
